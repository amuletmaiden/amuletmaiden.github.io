name: Export dragon head rebuild A review

on:
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: dragon-head-rebuild-A-review
          path: kt-bus-review/dragon-head-rebuild-A/*.jpg
          retention-days: 1
